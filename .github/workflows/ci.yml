name: CI

# ---------------------------------------------------------------------------
# Continuous Integration
# Runs on every push and pull request targeting `main`.
# Responsibilities: restore -> build -> test.
# The CD workflow (cd.yml) is triggered automatically once this succeeds.
# ---------------------------------------------------------------------------

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel superseded runs on the same ref to save CI minutes.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  # Single source of truth for the SDK version.
  # NOTE: DevOps.csproj currently targets net10.0. Set this to match your
  # <TargetFramework> ('8.0.x' for net8.0, '10.0.x' for net10.0).
  DOTNET_VERSION: '8.0.x'
  DOTNET_CONFIGURATION: 'Release'
  # Speed up runs and keep logs clean.
  DOTNET_CLI_TELEMETRY_OPTOUT: 'true'
  DOTNET_NOLOGO: 'true'
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: 'true'

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      # 1. Pull the repository contents onto the runner.
      - name: Checkout source
        uses: actions/checkout@v4

      # 2. Install the requested .NET SDK.
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      # 3. Cache the NuGet package store keyed on lock/project files
      #    so restores are fast on subsequent runs.
      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: nuget-${{ runner.os }}-${{ hashFiles('**/*.csproj') }}
          restore-keys: nuget-${{ runner.os }}-

      # 4. Restore NuGet dependencies.
      - name: Restore dependencies
        run: dotnet restore

      # 5. Compile. --no-restore avoids a redundant second restore.
      - name: Build
        run: dotnet build --configuration $DOTNET_CONFIGURATION --no-restore

      # 6. Run tests. Won't fail the build when no test project exists yet;
      #    add a *.Tests project and it is picked up automatically.
      - name: Test
        run: |
          if ls **/*[Tt]est*.csproj >/dev/null 2>&1; then
            dotnet test --configuration $DOTNET_CONFIGURATION --no-build \
              --verbosity normal --logger "trx;LogFileName=test-results.trx"
          else
            echo "No test projects found — skipping. Add one to enable tests."
          fi

      # 7. Publish test results as an artifact when they exist.
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: '**/test-results.trx'
          if-no-files-found: ignore
